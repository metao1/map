<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        tools:ignore="RtlHardcoded"
        android:theme="@style/AppTheme.DetailActivity"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <android.support.design.widget.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            android:fitsSystemWindows="true">
        <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:gravity="top"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:minHeight="?attr/actionBarSize"
                android:background="?attr/colorPrimary"
                android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
                app:popupTheme="@style/AppTheme.PopupOverlay">
        </android.support.v7.widget.Toolbar>
    </android.support.design.widget.AppBarLayout>
    <android.support.design.widget.FloatingActionButton
            android:layout_weight="0.21"
            android:id="@+id/driver_fab"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_margin="12dp"
            android:clickable="false"
            android:elevation="5dp"
            app:layout_anchor="@id/toolbar"
            app:layout_anchorGravity="end|right|end"
            android:fitsSystemWindows="true"
            android:src="@drawable/driver1600"/>
    <android.support.v4.widget.NestedScrollView
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            tools:showIn="@layout/driver_detail"
            android:layout_width="300dp"
            android:layout_height="400dp"
            android:paddingEnd="@dimen/not_so_huge_padding"
            android:paddingLeft="@dimen/not_so_huge_padding"
            android:paddingRight="@dimen/not_so_huge_padding"
            android:paddingTop="@dimen/not_so_huge_padding"
            android:scrollbars="vertical"
            android:fillViewport="true"
            android:keepScreenOn="true"
            android:scrollbarStyle="insideInset">
        <android.support.v7.widget.RecyclerView
                android:id="@+id/driver_recycle_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:scrollbars="vertical"/>
    </android.support.v4.widget.NestedScrollView>
    <include layout="@layout/driver_detail_footer"/>
</android.support.design.widget.CoordinatorLayout>
